<%= labelled_form_for(@ticket) do |f| %>
  <%= f.errors %>

  <%= f.text_field :title %>
  <%= f.text_area :description %>
  
  <%= f.actions "Do it" do %>
    <%= link_to "Cancel", :back %>
  <% end %>
<% end %>
